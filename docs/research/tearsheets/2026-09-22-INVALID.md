# 2026-09-22 tear sheets are not AI results

Date: 2026-09-23

Every HTML file in this folder dated 2026-09-22 is invalid as an AI strategy result. Leave the HTML in place. Do not cite those curves, share counts, or returns as proof that an agent traded.

What those runs actually were:

- They used `filing_rule` and `price_rule` from `proof_modes.py`.
- They spent $0 on model tokens.
- Python submitted the orders. Most stock fills were 1 share. Option proofs were 1 contract. Spreads closed on a timer.

`proof_modes.py` is deleted. The example strategies no longer have an execution mode. The only path is `initialize` plus `on_trading_iteration`: research, bull and bear together, an interpreter, then the trading agent. The trading agent is the only order path.
