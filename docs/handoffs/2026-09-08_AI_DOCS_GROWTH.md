# AI documentation discovery and reproducibility

Scope: source changes on the existing version/4.5.92 branch. No documentation,
package, Development or production deployment is included.

The AI hub now leads with four complete workflow routes. The example gallery
groups stocks, macro teams and options, distinguishes data requirements from
recorded runtime evidence, and uses all four current Data-On marketplace links.
The stock and ORB tutorials include installation, model, credentials, symbols,
dates, commands and expected artifacts. They explicitly do not claim a newly
completed model-backed run. The public Backtrader migration page is the single
maintained source; the old Markdown guide links to it.

Homepage route clicks emit `lumibot_docs_start_click` with route, destination
path and fragment. Partnership-page email links emit a separate contact event
without the email address or query string. Existing pageview/outbound events
remain. BotSpot classification now compares hostnames instead of substrings.
Localhost does not emit production analytics. A deterministic JavaScript VM
test checks duplicate loading, each destination, contact behavior and deceptive
outbound URLs.

Validation: 14 focused documentation/example tests passed; seven existing
budget-control tests passed; Sphinx HTML build succeeded. Existing broker
autodoc warnings are outside this change. Generated pages and local destinations
were checked, and Firefox desktop/mobile screenshots inspected. Mobile tables
scroll inside their containers. Furo emits an existing empty-getElementById
warning; no browser errors were observed on the inspected pages.

Dependencies still outstanding: approved communications connector for the
prepared internal handoff; authenticated Search Console for actual page/query
metrics; explicit bounded model-spend authority before fresh stock/ORB runs.
The distribution packet is a draft and has no new trading-result screenshots.
None of these dependencies should be represented as completed publication or
current backtest success. Company outreach belongs to the delegated colleague,
not to this documentation implementation task.
