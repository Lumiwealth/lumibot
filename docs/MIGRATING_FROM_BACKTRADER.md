# Migrating from Backtrader to LumiBot

The maintained guide now lives in [the public documentation source](../docsrc/MIGRATING_FROM_BACKTRADER.rst).
It includes lifecycle mapping, a complete LumiBot allocation example, order/data parity checks, and the separate broker runner.

Edit that source rather than maintaining a second comparison here. The generated documentation route is `MIGRATING_FROM_BACKTRADER.html`.
